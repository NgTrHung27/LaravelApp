<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Index function of Products</title>
    <h1>Index function of Products</h1>
    {{-- <h2> Title: {{ $title }}</h2>
    <h3>{{ $x }}</h3>
    <h3>{{ $y }}</h3>
    <h3>{{ $z }}</h3> --}}
    {{-- <h3>{{ $name }}</h3> --}}

    {{-- @foreach ($myphone as $item)
        <h3>{{ $item }}</h3>
    @endforeach --}}
    {{-- @foreach ($product as $item)
        <h3>{{ $item }}</h3>
    @endforeach --}}
    {{-- <h3>{{ $product}} --}}
    <a href="{{ route('products') }}">Link to products</a>

</head>
<body>

</body>
</html>
